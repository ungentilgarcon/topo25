const { expect } = require('chai')

// C3 was removed in favor of Recharts. Keep this file as a placeholder so historic test paths remain valid.
describe.skip('buildC3Options (removed)', () => {
  it('was removed during migration to Recharts', () => {
    expect(true).to.equal(true)
  })
})
